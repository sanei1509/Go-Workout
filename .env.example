# Database Configuration
# Reemplaza con tu URL de PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/go_workout?schema=public"

# Ejemplos de DATABASE_URL:
# Local PostgreSQL: postgresql://postgres:postgres@localhost:5432/go_workout?schema=public
# Docker: postgresql://postgres:postgres@localhost:5432/go_workout?schema=public
# Heroku/Render: (copiar del dashboard)
# Supabase: (copiar del dashboard)

# OpenAI API Key (OPCIONAL)
# Si no se configura, la app usará rutinas de fallback
# Obtén tu API key en: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-your-api-key-here"

# Next.js (auto-configurado)
# No es necesario modificar estas variables en desarrollo
# NEXT_PUBLIC_API_URL="http://localhost:3000"
